package com.dndmaster.adventure.application.runtime;

/** Accepted ways in which a player turn can move the fiction forward. */
public enum MeaningfulProgressCategory {
    WORLD_STATE,
    INFORMATION,
    OUTCOME,
    DECISION,
    CHECK,
    PROGRESS
}
